<div class="row">
  <div class="twelve columns">
    <h1>Browse Questions by Tag</h1>
  </div>
</div>

<ul class="tags u-full-width">
  <% @tags.each do |tag| %>
    <li><%= render partial: 'tag', locals: { tag: tag } %></li>
  <% end %>
</ul>
