<div class="row">
  <div class="twelve columns">
    <h1>Browse All Questions Tagged <%= @tag.text.upcase %></h1>
 </div>
</div>

<ul class="tags u-full-width">
  <% @questions.each do |question| %>
    <%= render partial: '/questions/question', locals: { question: question } %>
  <% end %>
</ul>
